[project]
name = "Turm"
version = "0.1.0"
description = "Default template for PDM package"
authors = [
    {name = "lab260", email = "a.i.mokin@mtuci.ru"},
]
dependencies = ["fastapi>=0.119.0", "pydantic>=2.12.3", "pydantic-settings>=2.11.0", "sqlalchemy>=2.0.44", "aiokafka>=0.12.0"]
requires-python = "==3.13.*"
readme = "README.md"
license = {text = "None"}


[tool.pdm]
distribution = false
